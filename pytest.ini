[pytest]
# Anchor rootdir here and put the project root on sys.path so tests can
# `from logic_utils import ...` no matter how/where pytest is invoked
# (IDE, plain `pytest`, or from another working directory).
pythonpath = .
testpaths = tests
